// Replace this IP with your PC's local IP address
export const SERVER_IP = '192.168.1.100'; // Example IP - you need to change this
export const SERVER_PORT = 8082;

export const WS_URL = `ws://${SERVER_IP}:${SERVER_PORT}`;
export const API_URL = `http://${SERVER_IP}:${SERVER_PORT}/api`; 